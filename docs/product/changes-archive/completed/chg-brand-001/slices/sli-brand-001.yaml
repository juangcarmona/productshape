schema: product-definition-as-code/delivery-slice/v1alpha1
id: SLI-BRAND-001
title: Rename public assets to the ProductShape brand
status: completed
product-change: CHG-BRAND-001
outcome: >-
  A newcomer meets the product publicly as ProductShape — the repository, the package namespace, the binary, the documentation title and every public reference present the ProductShape brand as the reference implementation of the Product Definition as Code methodology — while every existing behaviour, contract and command keeps working unchanged.
implements:
  - requirement: CON-BRAND-001
    coverage: full
affects: []
depends-on: []
verification:
  - The repository, npm scope, CLI binary, documentation title and public references present ProductShape; the methodology is still named Product Definition as Code and the two read as distinct layers.
  - The primary binary is prodshape; product-definition still works as a v0.x compatibility alias producing identical output.
  - prodshape validate and doctor stay green on this repository, and the artifact and graph semantics are unchanged.
  - The .product/ directory, the product-definition-as-code/... schema identifiers and the PRODUCT### diagnostic codes are unchanged.
  - Regenerated Claude and Copilot assets carry the ProductShape brand and expose the /ps:* alias alongside the canonical /product:* namespace, with no managed-file drift.
out-of-scope:
  - Removing the product-definition binary alias and updating the model use cases that name it (a follow-up Product Change before v1).
  - Publishing any package to npm.
  - Any change to methodology semantics, schema identifiers, diagnostic codes or the .product/ configuration directory.
