---
description: Continue and graph-refine the named ProductShape Product Change in place.
---

# Refine Product Change

Resolve the named container under `.specify/productshape/changes/<name>/`. Re-read its `proposal.md`, current delta, `impact.json` and unresolved questions, then perform the proportional graph-guided clarification pass and submit the resulting JSON through `prodshape speckit-product refine <name> --input <file>`. Keep checked-and-excluded neighbours visible, preserve human decisions as rationale, and do not create a second lifecycle. Update only that container; never approve, apply, archive, or edit the accepted model.
