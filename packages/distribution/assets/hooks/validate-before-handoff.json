{
  "schema": "product-definition-as-code/hook/v1alpha1",
  "name": "validate-before-handoff",
  "description": "Ensure the baseline and the Product Change overlay validate before a handoff is generated.",
  "triggers": [
    {
      "event": "before-handoff-generation",
      "run": ["product-definition validate", "product-definition change validate ${change}"]
    }
  ],
  "blocking": true,
  "notes": "A handoff generated from an invalid overlay would package broken product knowledge; this hook blocks on deterministic failure."
}
