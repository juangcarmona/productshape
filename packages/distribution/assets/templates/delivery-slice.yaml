# Delivery slice: an implementable, verifiable product increment of one Product Change.
# A good slice preserves meaningful product behaviour through the stack; it is not a
# technical layer, an endpoint or a screen. AI may propose slices; a human approves them.
# When coverage is partial, the entry must include a precise scope field.
# Contract: docs/specification/delivery-slices.md
# Schema reference: docs/specification/frontmatter-reference.md#delivery-slice
schema: product-definition-as-code/delivery-slice/v1alpha1
id: SLI-EXAMPLE-001
title: Example Delivery Slice
status: draft
product-change: CHG-EXAMPLE-001
outcome: What an actor can do after this slice that they could not do before.
implements:
  - requirement: FR-EXAMPLE-001
    coverage: full
affects: []
depends-on: []
verification:
  - How this increment is verified, in product terms.
out-of-scope:
  - What this slice deliberately does not deliver.
