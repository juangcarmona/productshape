# Minimal example

The smallest coherent product model: one artifact of every kind, describing a deliberately tiny
imaginary product (a link-shortening service). Use it to see each contract in isolation or as a
starting point for a new model. The conformance suite validates this example on every run.

Layout mirrors a consumer repository's `docs/product/model/`.
