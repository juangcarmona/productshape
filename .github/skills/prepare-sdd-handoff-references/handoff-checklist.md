<!-- MANAGED FILE - generated by prodshape (Product Definition as Code v0.1.0)
     from skills/prepare-sdd-handoff/references/handoff-checklist.md. Do not edit by hand; run: prodshape integration update -->

# Handoff checklist

## Pre-flight (before `handoff create`)

- Slice file read; `status: approved` confirmed — never generate from `draft`/`proposed`
  (PRODUCT040), never edit the status to unblock generation.
- Slice's `product-change` matches the change directory containing it.
- `prodshape change validate <CHG-ID>` exits 0; errors stop the handoff.
- Work-item reference confirmed in `<provider>:<owner>/<repo>#<id>` form, pointing at a backlog
  item the team already created; no tracker API calls.
- Destination confirmed with the human: `--out <dir>`, or the adapter plus the exact native SDD
  change name the sidecars land inside.
- Working tree clean enough that `git rev-parse HEAD` reflects the artifacts being handed off —
  uncommitted edits to referenced artifacts make `source.revision` a lie.

## Post-flight (after `handoff create`)

- `prodshape handoff status <path>` reports `current`; `stale` or `invalid` right after
  generation means an upstream problem — report the named artifacts, do not patch files by hand.
- Generated files present at the destination: `product-handoff.yaml`, `product-context.md`, and
  `product-coverage.yaml` when the adapter placed sidecars.
- `product-context.md` opens with the generated marker naming the handoff ID and contains no
  technical design.
- Every unresolved question from the change's `## Open Questions` is quoted in the summary to
  the SDD side, verbatim — they are decisions the implementation must not silently make.
- Summary names the handoff ID, slice outcome, implemented requirements with partial-coverage
  scopes, and restates the boundary: SDD reports back; it never rewrites product knowledge and
  never promotes.
