# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo

# Test output
coverage/

# Derived, regenerable outputs (never canonical) — at the root and in runnable examples.
**/.product/generated/
**/.product/cache/

# Archived change drafts: the PR is the record, not the draft.
docs/product/changes/archive/

# Editor and OS noise
.DS_Store
Thumbs.db

# Staged into the publishable CLI at build time (copies of canonical schemas/ and distribution assets).
packages/cli/schemas/
packages/cli/assets/
packages/cli/*.tgz

# vscode settings
.vscode/