---
'@prodshape/cli': patch
---

Graduate the CLI to a stable release: promote `0.1.0-alpha.1` to `0.1.0` on the `latest` dist-tag.
This is the first stable CLI now that the supporting packages (`@prodshape/core`, `distribution`,
`adapter-openspec`, `integration-claude`, `integration-copilot`) are published at `0.1.0`.
