---
'@prodshape/cli': minor
---

Add `prodshape --version`, sourced from the installed CLI package manifest, and enforce the primary documented init and citation-drift quickstart against a packed tarball installed without workspace links.
